"use server";

export const signIn = async () => {
  try {
    // All Functionality for Server Actions Sign-in
  } catch (e) {
    console.error(e.toString());
  }
};

export const signUp = async (userData: SignUpParams) => {
  try {
    // All Functionality for Server Actions Sign-up
  } catch (e) {
    console.error(e.toString());
  }
};
